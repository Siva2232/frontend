import React from 'react';
import ServiceTracker from './ServiceTracker';

// ServiceDashboard is an alias for ServiceTracker to isolate service-team view
const ServiceDashboard = () => <ServiceTracker />;

export default ServiceDashboard;
